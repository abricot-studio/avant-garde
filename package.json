{
  "private": true,
  "scripts": {
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "prettier": "prettier --check .",
    "prettier:write": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "vercel": "^21.3.3"
  }
}
