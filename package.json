{
  "name": "@arbart/monorepo",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "prettier": "prettier --check .",
    "prettier:write": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "lerna": "^4.0.0"
  }
}
